---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/layout/AppSidebar.vue
  - app/stores/useSaveStore.ts
autonomous: true
requirements:
  - NAV-01
  - STATE-02

must_haves:
  truths:
    - "Sidebar checks save store state before navigation"
    - "Pinia store state is accessible to sidebar components"
  artifacts:
    - path: app/components/layout/AppSidebar.vue
      provides: Sidebar navigation component
      min_lines: 100
    - path: app/stores/useSaveStore.ts
      provides: Save file state management
      exports:
        - isLoaded
        - foundItemIds
        - selectedCharacter
  key_links:
    - from: AppSidebar.vue
      to: useSaveStore()
      via: storeToRefs
      pattern: storeToRefs.*useSaveStore
---

<objective>
Analyze current sidebar and routing logic to understand how navigation works and where smart redirects need to be implemented.

Purpose: Establish baseline understanding before implementing navigation guards per D-01 (hybrid approach)
Output: Analysis complete, readiness for implementation
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@app/stores/useSaveStore.ts
@app/components/layout/AppSidebar.vue

# Routes in app
# - / (index) - landing with upload
# - /inventory/{category} - progress view
# - /archives/{category} - encyclopedia view
# - /wiki/{category}/{id} - item detail
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze sidebar navigation structure</name>
  <files>app/components/layout/AppSidebar.vue</files>
  <action>
    Read AppSidebar.vue and confirm:
    1. Current navigation uses NuxtLink components to /inventory/{item.key}
    2. No state checking before navigation
    3. Items computed derives from store.stats
    
    Document findings in the summary.
  </action>
  <verify>
    <automated>grep -c "NuxtLink\|isLoaded\|storeToRefs" app/components/layout/AppSidebar.vue</automated>
  </verify>
  <done>Sidebar structure documented: NuxtLink with no state check</done>
</task>

<task type="auto">
  <name>Task 2: Verify Pinia store exports</name>
  <files>app/stores/useSaveStore.ts</files>
  <action>
    Confirm useSaveStore exports:
    - isLoaded (ref<boolean>)
    - foundItemIds (ref<string[]>)
    - selectedCharacter (computed)
    - stats (computed)
    - globalStats (computed)
    
    These are the primitives needed for navigation guards per D-01.
  </action>
  <verify>
    <automated>grep -c "isLoaded\|foundItemIds\|selectedCharacter\|stats" app/stores/useSaveStore.ts</automated>
  </verify>
  <done>Store exports confirmed: isLoaded, foundItemIds available</done>
</task>

<task type="auto">
  <name>Task 3: Check existing page routes</name>
  <files>app/pages/index.vue, app/pages/inventory, app/pages/archives</files>
  <action>
    Verify page structure:
    - index.vue exists (landing with file upload)
    - /inventory/[category].vue exists (progress view)
    - /archives/[category].vue exists (encyclopedia)
    
    Document which routes exist for redirect targeting.
  </action>
  <verify>
    <automated>ls app/pages/*.vue app/pages/*/</automated>
  </verify>
  <done>Routes confirmed: index, inventory/[category], archives/[category]</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client-side only} | No external API, all local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial | SSR hydration | accept | Client-only app, no server resources |
</threat_model>

<verification>
- [x] Sidebar uses NuxtLink for navigation
- [x] Store provides isLoaded state
- [x] Routes exist: /inventory, /archives
</verification>

<success_criteria>
Analysis complete — ready to implement smart redirects in 01-02
</success_criteria>

<output>
After completion, create `.planning/phases/01-navigation/01-01-SUMMARY.md`
</output>
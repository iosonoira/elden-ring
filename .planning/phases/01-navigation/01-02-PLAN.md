---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/components/layout/AppSidebar.vue
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - STATE-01

must_haves:
  truths:
    - "No save loaded → click inventory → redirect to /archives"
    - "Save loaded → click inventory → stay on /inventory"
    - "Sidebar checks isLoaded before navigation (per D-01)"
  artifacts:
    - path: app/components/layout/AppSidebar.vue
      provides: Sidebar with smart redirect
      min_lines: 130
      must_have: click handler with isLoaded check
  key_links:
    - from: sidebar item click
      to: isLoaded check
      via: computed redirect target
      pattern: isLoaded.*archives|inventory
---

<objective>
Implement smart redirect in sidebar per D-01 (hybrid approach): sidebar click handlers check isLoaded before navigation.

Purpose: Per D-02, users CAN manually navigate anywhere, but smart defaults guide them. When clicking Inventory with no save → Archives (encyclopedia). When save loaded → stay on Inventory (progress).
Output: Modified AppSidebar.vue with redirect logic
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/01-navigation/01-01-SUMMARY.md
@app/stores/useSaveStore.ts
@app/components/layout/AppSidebar.vue

# Decisions from CONTEXT.md:
# - D-01: Hybrid approach — sidebar click handlers check isLoaded before navigation
# - D-02: Users can manually navigate to any section (no forced redirect)
# - D-03: Use Pinia's default reactivity (no localStorage plugin for v1)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add redirect logic to sidebar navigation</name>
  <files>app/components/layout/AppSidebar.vue</files>
  <behavior>
    - When user clicks inventory link AND !isLoaded → navigate to /archives/{category}
    - When user clicks inventory link AND isLoaded → navigate to /inventory/{category}
    - When user clicks archives link → always navigate to /archives/{category}
    - Users can manually override by typing URL (D-02 honored)
  </behavior>
  <action>
    Modify AppSidebar.vue:
    
    1. Add computed redirect target function:
    ```typescript
    const getInventoryTarget = (category: string) => {
      return isLoaded.value 
        ? localePath(`/inventory/${category}`)
        : localePath(`/archives/${category}`)
    }
    ```
    
    2. Replace :to="localePath(`/inventory/${item.key}`)" with :to="getInventoryTarget(item.key)"
    
    3. Keep archives links unchanged (always go to /archives)
    
    This implements D-01 (hybrid approach) while honoring D-02 (no forced redirect — users can type URL directly).
  </action>
  <verify>
    <automated>grep -c "getInventoryTarget\|isLoaded.*archives\|archives.*category" app/components/layout/AppSidebar.vue</automated>
  </verify>
  <done>Navigation links check isLoaded, redirect to archives when no save</done>
</task>

<task type="auto">
  <name>Task 2: Add visual indicator for redirect behavior</name>
  <files>app/components/layout/AppSidebar.vue</files>
  <action>
    Add tooltip or hint showing where link will go:
    
    - Add title attribute or aria-label showing redirect destination
    - Example: "Weapons — Click to view (redirects to Archives if no save loaded)"
    
    This improves UX by making the smart redirect visible.
  </action>
  <verify>
    <automated>grep -c "title\|aria-label\|redirect" app/components/layout/AppSidebar.vue</automated>
  </verify>
  <done>Navigation links have visible redirect indicator</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client-side only} | No external API, all local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | Navigation | accept | User controls browser, no security impact |
</threat_model>

<verification>
- [x] Inventory links check isLoaded state
- [x] No save → redirect to /archives
- [x] Save loaded → stay on /inventory
- [x] Archives links unchanged
</verification>

<success_criteria>
Smart redirect working: clicking Inventory with no save → Archives; clicking Inventory with save loaded → Inventory
</success_criteria>

<output>
After completion, create `.planning/phases/01-navigation/01-02-SUMMARY.md`
</output>
---
phase: "01"
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - app/stores/useSaveStore.ts
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03

must_haves:
  truths:
    - "Save file data persists across page navigation"
    - "Pinia store retains data on route change"
    - "No state reset issues on navigation"
  artifacts:
    - path: app/stores/useSaveStore.ts
      provides: Save file state with persistence
      must_have: No reset on navigation
    - path: app/app.vue or nuxt.config.ts
      provides: Pinia configuration
      must_have: persist: false (default per D-03)
  key_links:
    - from: Pinia store
      to: navigation
      via: client-side only
      pattern: isLoaded stays true across route
---

<objective>
Verify and fix state persistence across route navigation. Ensure Pinia store retains data when users navigate between pages.

Purpose: Per D-03, use Pinia's default reactivity (no localStorage plugin). Need to verify this works correctly and fix any issues.
Output: State persistence verified/fixed
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/stores/useSaveStore.ts
@app/app.vue
@.planning/ROADMAP.md

# D-03: Use Pinia's default reactivity (no localStorage plugin for v1)
# Per RESEARCH.md:
# - SSR hydration timing issues possible
# - Lazy-loaded routes may cause state reset
# - Hard refresh clears state (expected behavior)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Pinia persistence configuration</name>
  <files>app/app.vue, app/nuxt.config.ts</files>
  <action>
    Check Pinia setup in app:
    
    1. Verify pinia plugin is installed in nuxt.config.ts or app.vue
    2. Verify NO persist plugin is loaded (per D-03)
    3. Document current configuration
    
    Pinia default behavior: in-memory only. This is correct per D-03.
  </action>
  <verify>
    <automated>grep -c "pinia\|PiniaVuePlugin\|createPinia" app/</automated>
  </verify>
  <done>Pinia configured, no persist plugin (as per D-03)</done>
</task>

<task type="auto">
  <name>Task 2: Test state persistence across navigation</name>
  <files>app/stores/useSaveStore.ts</files>
  <action>
    Manual verification test:
    
    1. Load save file on / (index)
    2. Navigate to /inventory/armament
    3. Verify isLoaded still true
    4. Navigate to /archives/armament
    5. Verify isLoaded still true
    
    Document any state reset issues found.
  </action>
  <verify>
    <automated>MISSING — manual browser test required</automated>
  </verify>
  <done>State persists across navigation</done>
</task>

<task type="auto">
  <name>Task 3: Fix SSR hydration issues if found</name>
  <files>app/stores/useSaveStore.ts, app/app.vue</files>
  <action>
    If SSR causes issues (CLIENT_ONLY or useState patterns may be needed):
    
    1. Use $fetch/browse to trigger client-side only mode
    2. Use Nuxt's useState with Pinia if needed
    3. Wrap client-only logic in onMounted or useCookie
    
    Per RESEARCH.md: "SSR hydration timing" is an edge case to handle.
  </action>
  <verify>
    <automated>grep -c "onMounted\|useState\|ClientOnly" app/stores/useSaveStore.ts</automated>
  </verify>
  <done>SSR edge cases handled</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client-side only} | No external API, all local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Repudiation | State persistence | accept | User accepts client-only limitation |
</threat_model>

<verification>
- [x] Pinia configured (no persist plugin, per D-03)
- [x] State persists across route change
- [x] SSR edge cases handled
</verification>

<success_criteria>
State persists across navigation. Hard refresh clears state (expected, documented in RESEARCH.md).
</success_criteria>

<output>
After completion, create `.planning/phases/01-navigation/01-03-SUMMARY.md`
</output>
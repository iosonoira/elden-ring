---
phase: 01-ui-animations
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/app.vue
  - app/layouts/default.vue
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Page navigation triggers smooth fade/slide transitions"
    - "Enter and leave transitions apply to all routes"
  artifacts:
    - path: "app/app.vue"
      provides: "Global page transition wrapper using <Transition>"
      exports: ["NuxtPage with transition"]
  key_links:
    - from: "app/app.vue"
      to: "app/assets/scss/abstracts/_animations.scss"
      via: "CSS classes .page-enter-active, .page-leave-active"
---

<objective>
Implement Vue `<Transition>` components for page navigation with fade and slide effects. Wrap all routes with transition animation.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/app.vue
@app/layouts/default.vue
@app/assets/scss/abstracts/_animations.scss (created in plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add page transition to app.vue</name>
  <files>app/app.vue</files>
  <action>
    Modify app.vue to wrap NuxtPage with <Transition>:
    
    ```vue
    <template>
      <NuxtLayout>
        <NuxtPage />
      </NuxtLayout>
    </template>
    ```
    
    Then add page transition CSS in a new file `app/assets/scss/base/_transitions.scss`:
    - `.page-enter-active` / `.page-leave-active` with $transition-slow timing
    - Use custom $ease-out-expo from _animations.scss
    - Enter: fade + slide-up (20px)
    - Leave: fade + slide-down (10px)
    Import _transitions.scss in main.scss.
  </action>
  <verify>grep -c "page-enter-active\|page-leave-active" app/assets/scss/base/_transitions.scss</verify>
  <done>All route changes trigger fade-slide transition animation</done>
</task>

</tasks>

<verification>
Verify transitions work on route change by navigating between pages.
</verification>

<success_criteria>
- Entering pages fade in with upward slide
- Leaving pages fade out with downward slide
- Transition uses custom easing from _animations.scss
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-animations/01-02-SUMMARY.md`
</output>
---
phase: 01-ui-animations
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - app/components/wiki/ItemGrid.vue
  - app/assets/scss/components/_item-grid.scss
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Grid items appear with staggered delay animation"
    - "Each item delays based on its index"
  artifacts:
    - path: "app/components/wiki/ItemGrid.vue"
      provides: "Grid wrapper with TransitionGroup for stagger effect"
      exports: ["TransitionGroup with stagger delay CSS"]
  key_links:
    - from: "app/components/wiki/ItemGrid.vue"
      to: "app/assets/scss/abstracts/_animations.scss"
      via: "animation-delay calculation in style"
---

<objective>
Add staggered animations for item grids and lists using Vue TransitionGroup with computed animation-delay based on item index.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/components/wiki/ItemGrid.vue
@app/components/wiki/ItemCard.vue
@app/assets/scss/components/_item-grid.scss
@app/assets/scss/abstracts/_animations.scss (created in plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap ItemGrid with TransitionGroup</name>
  <files>app/components/wiki/ItemGrid.vue</files>
  <action>
    Modify ItemGrid.vue to use TransitionGroup instead of v-for directly:
    
    ```vue
    <script setup lang="ts">
    import { useWikiStore } from '~/stores/useWikiStore';
    
    interface Item {
      id: string
      name: string
      image?: string
    }
    
    const props = defineProps<{
      items: Item[]
      category: string
    }>()
    
    // Stagger delay calculation: 50ms base delay per item
    const getItemDelay = (index: number) => `${index * 50}ms`
    </script>
    
    <template>
      <TransitionGroup 
        tag="div" 
        name="item-grid"
        class="item-grid"
      >
        <LazyWikiItemCard 
          v-for="(item, index) in items" 
          :key="item.id" 
          :item="item"
          :category="category"
          :style="{ '--delay': getItemDelay(index) }"
        />
      </TransitionGroup>
    </template>
    ```
    
    The --delay CSS variable enables staggered animation in SCSS.
  </action>
  <verify>grep -c "TransitionGroup\|--delay" app/components/wiki/ItemGrid.vue</verify>
  <done>ItemGrid uses TransitionGroup with dynamic delay variable</done>
</task>

<task type="auto">
  <name>Task 2: Add stagger animation to _item-grid.scss</name>
  <files>app/assets/scss/components/_item-grid.scss</files>
  <action>
    Add TransitionGroup animation styles:
    
    ```scss
    @use '../abstracts/variables' as *;
    @use '../abstracts/animations';
    
    .item-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: $space-4;
    }
    
    // TransitionGroup items
    .item-grid-enter-active,
    .item-grid-leave-active {
      transition: opacity 400ms $ease-out-expo, transform 400ms $ease-out-expo;
      transition-delay: var(--delay, 0ms);
    }
    
    .item-grid-enter-from {
      opacity: 0;
      transform: translateY(20px) scale(0.95);
    }
    
    .item-grid-leave-to {
      opacity: 0;
      transform: scale(0.95);
    }
    
    // Prevent layout shift during leave
    .item-grid-leave-active {
      position: absolute;
    }
    ```
  </action>
  <verify>grep -c "item-grid-enter\|TransitionGroup\|--delay" app/assets/scss/components/_item-grid.scss</verify>
  <done>Staggered animation CSS applied to item-grid TransitionGroup</done>
</task>

</tasks>

<verification>
Verify items appear one-by-one with ~50ms stagger when grid loads.
</verification>

<success_criteria>
- Items animate in sequentially with stagger effect
- First item appears at 0ms, second at 50ms, etc.
- Transition uses custom $ease-out-expo
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-animations/01-03-SUMMARY.md`
</output>
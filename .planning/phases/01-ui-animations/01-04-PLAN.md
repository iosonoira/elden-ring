---
phase: 01-ui-animations
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
files_modified:
  - app/components/wiki/ItemCard.vue
  - app/assets/scss/components/_item-card.scss
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "Cards lift on hover with subtle scale"
    - "Gold glow intensifies on hover"
    - "Focus states have visible indication"
  artifacts:
    - path: "app/components/wiki/ItemCard.vue"
      provides: "Card with hover/focus interactive states"
      exports: ["Interactive classes"]
    - path: "app/assets/scss/components/_item-card.scss"
      provides: "Hover/focus animation styles"
  key_links:
    - from: "app/components/wiki/ItemCard.vue"
      to: "_mixins.scss"
      via: "gold-glow-box mixin"
---

<objective>
Enhance hover/focus micro-interactions on ItemCard with lift transform, gold glow intensification, and smooth transitions.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/TS/elden-ring/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo\Desktop/TS/elden-ring/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/components/wiki/ItemCard.vue
@app/assets/scss/abstracts/_mixins.scss
@app/assets/scss/abstracts/_variables.scss
@app/assets/scss/abstracts/_animations.scss (created in plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive class to ItemCard</name>
  <files>app/components/wiki/ItemCard.vue</files>
  <action>
    Update ItemCard.vue to add interactive class. The component already has 'item-card' class. Ensure 'tabindex="0"' is on the card for focus:
    
    Verify existing template has:
    - Root element has class "item-card glass-panel"
    - Add tabindex="0" for keyboard focus: `<div ref="cardRef" class="item-card glass-panel" tabindex="0">`
    - Add keyboard handlers: `@keydown.enter="openWiki"` and `@keydown.space.prevent="openWiki"`
    - Add openWiki function that navigates to wiki link.
    
    Current template is fine — focus comes from the link inside. Ensure `.item-card` has `:focus-visible` styling in SCSS.
  </action>
  <verify>grep -c "tabindex\|item-card" app/components/wiki/ItemCard.vue | measure-Object -Line</verify>
  <done>ItemCard is focusable via keyboard navigation</done>
</task>

<task type="auto">
  <name>Task 2: Add hover/focus animations to _item-card.scss</name>
  <files>app/assets/scss/components/_item-card.scss</files>
  <action>
    Read existing _item-card.scss first, then add:
    
    ```scss
    @use '../abstracts/variables' as *;
    @use '../abstracts/animations';
    @use '../abstracts/mixins' as *;
    
    .item-card {
      @include glass-panel;
      transition: 
        transform $transition-base $ease-out-expo,
        box-shadow $transition-base $ease-out-expo,
        border-color $transition-base;
      cursor: pointer;
      
      &:hover {
        transform: translateY(-4px) scale(1.02);
        @include gold-glow-box(0.25);
        border-color: rgba($color-primary, 0.25);
      }
      
      &:focus-visible {
        outline: 1px solid $color-primary;
        outline-offset: 2px;
        transform: translateY(-2px);
      }
      
      // Initial load animation
      animation: slide-up 400ms $ease-out-expo both;
    }
    ```
    
    NOTE: Existing _item-card.scss may have styles. Merge carefully.
  </action>
  <verify>grep -c "translateY\|gold-glow-box\|focus-visible" app/assets/scss/components/_item-card.scss</verify>
  <done>Hover lift, glow intensification, and focus-visible styles applied</done>
</task>

</tasks>

<verification>
Verify hover triggers lift and glow on mouse enter.
</verification>

<success_criteria>
- Card lifts 4px on hover with scale(1.02)
- Gold glow intensifies from 0.15 to 0.25 strength
- Focus-visible shows gold outline for keyboard navigation
- All transitions use $ease-out-expo
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-animations/01-04-SUMMARY.md`
</output>